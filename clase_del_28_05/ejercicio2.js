const mostrar_arreglo = () => {
    let arreglo = [1,2,3,4,5,6];
    arreglo.push(7);
    arreglo.push(8);
    let cantidad_numeros = arreglo.lcngth;
    let suma=0;
    arreglo.forEach(elemento (
        console.log("Usando forEach:" + elemento);
        suma= suma_element;  
    ));
}